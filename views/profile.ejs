<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .profile-container {
            margin-top: 50px;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 10px;
        }

        .post-container {
            margin-top: 20px;
        }

        .post {
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 10px;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <%- include('_navbar') %>
    <div class="container">
        <h1>Profile</h1>
       
        <a href="/profile/edit" class="btn btn-primary">Edit Profile</a>

        <div class="profile-container">
            <div class="row">
                <div class="col-md-4">
                    <img src="<%= profile.avatarUrl %>" class="img-fluid" alt="Profile Picture">
                </div>
                <div class="col-md-8">
                    <h2><%= profile.User.username %></h2>
                    <p><strong>Full Name:</strong> <%= profile.fullName %></p>
                    <p><strong>Email:</strong> <%= profile.User.email %></p>
                    <p><strong>Bio:</strong> <%= profile.bio %></p>
                </div>
            </div>
        </div>

        <!-- Tampilkan semua post yang diposting oleh user -->
        <div class="post-container">
            <h2>Posts</h2>
            <% profile.User.Posts.forEach(post => { %>
                <div class="post">
                    <h3><%= post.title %></h3>
                    <p><%= post.content %></p>
                    <p><strong>Created At:</strong> <%= post.createdAt %></p> 
                   
                    <a href="/posts/delete/<%= post.id %>" class="btn btn-danger">Delete Post</a>
                </div>
            <% }); %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
